//contains the providers for authentication using next-auth

import type { NextAuthOptions } from "next-auth";

export const options: NextAuthOptions = {
    //providers for authentication
    providers: [],
}